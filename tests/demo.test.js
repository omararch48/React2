describe('Pruevas en el primer componente', () => { 
    
    test('prueba inicial', () => {
        const message1 = 'Mensaje';
        const message2 = message1.trim();
    
        // expect(message1).toBe(message2);
        expect(message1).toBe(message2)
    });
})
